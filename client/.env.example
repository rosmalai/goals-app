# Live backend URL (no trailing slash). Required on Render/Vercel for the static frontend.
# Example: https://your-goals-api.onrender.com
VITE_API_URL=

# Local dev: leave VITE_API_URL empty — Vite proxies /api to http://localhost:5000
